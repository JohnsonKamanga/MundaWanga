import { Text, View } from "react-native";

export default function Settings(){

    return(
        <View className="min-h-screen h-full w-full bg-red=500">
            <Text className="text-white">
                Your Settings
            </Text>
        </View>
    )
}